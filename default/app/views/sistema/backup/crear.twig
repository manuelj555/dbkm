{% extends not is_ajax ? "@templates/backend/default.twig" : "@templates/ajax.twig" %}

{% block content %}
    {{ form_open('', 'post', {'id':'form', 'class':'dw-validate'}) }}
        <div class="row-fluid">
            <div class="span6">
                {{ form_text('backup.denominacion', {'class':'input-xxlarge input-required input-alphanum mask-alphanum show-error'}, NULL, 'Denominación') }}
            </div>
        </div>
        <div class="row-fluid">
            <div class="form-actions">
                {{ DwButton_save('Crear copia de seguridad') }}
                {{ DwButton_reset('form') }}
                {{ DwButton_cancel() }}
            </div>
        </div>
    {{ form_close() }}
{% endblock content %}