{% extends not is_ajax ? "@templates/backend/default.twig" : "@templates/ajax.twig" %}

{% block content %}
    {{ form_open('', 'post', {id:'form', class:'dw-validate'}) }}

        <div class="row-fluid">
            <div class="span6">
                {{ form_date('log', {class:'input-required mask-date input-date show-errors'}, "now"|date("Y-m-d"), 'Día') }}
            </div>
        </div>

        <div class="row-fluid">
            <div class="form-actions">
                {{ DwButton_submit('Listar logs', 'check', null, 'mostrar') }}
                {{ DwButton_cancel() }}
            </div>
        </div>
    {{ form_close() }}
{% endblock content %}